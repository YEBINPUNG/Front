# 주제: 팀 프로젝트 협업, 회고 AI 매니저

## 주제 선택 이유:

기존 Notion 등 협업을 돕는 생산성 도구를 활용한 프로젝트 과정에서 AI 관련 기능을 사용하기 어려웠고, 회고가 잘 진행되지 않으며, 유실되는 태스크가 많아지는 등의 문제가 많았기 때문에

## 대상 사용자와 해결할 문제:

대상 사용자: 마이스터고 학생 및 대학생 프로젝트 팀

## 해결할 문제:

학생 팀 프로젝트는 회의에서 나온 결정이 실제 작업으로 추적되지 않아, 지연을 사후에만 인지한다. 이 서비스는 회의록에서 작업을 자동 추출하고, 진척 패턴으로 지연을 사전에 경고한다.

## 핵심 기능 3개:

1. 회의록 기반 태스크 변환 - AI가 회의록을 기반으로 할일 / 마감일 / 담당자 등의 태스크를 구조화하여 정리해준다.

2. 업무 보드 + 진척도 - 태스크당 담당자, 마감일, 예상 소요시간 등을 정리할 수 있도록 대시보드가 있다.

3. 태스크 지연 탐지 - AI가 태스크별 지연 위험을 확인하고, 근거를 표시해준다.

## AI 활용 아이디어:

1. 회의록 구체화 기능: AI가 회의록 원문을 바탕으로 프로젝트의 태스크를 정리해주거나, 회의록을 정리해준다.

2. 태스크 지연 탐지: 마감까지 남은 일수, 마지막 상태 변경 후 경과일, 담당자 동시 진행 작업 수, 작업 예상 소요시간 대비 경과 시간 등의 데이터를 기반으로 AI가 태스크 지연 확률을 예측하여 사전에 알려준다.

## 보안 적용 계획:

비밀번호는 bcrypt로 해싱하고 JWT 기반 로그인에 Owner/Member/Viewer 권한을 분리하며, 모든 API에서 프로젝트 소속 여부를 서버가 재검증해 비공개 프로젝트 접근을 차단한다. 입력값은 서버 측에서 검증하고 ORM과 출력 이스케이프로 SQL 인젝션·XSS를 막으며, 모든 변경은 수정 이력에 기록하고 삭제는 Soft Delete로 처리한다. 개인정보는 최소 수집하고 API 키는 환경변수·Secrets Manager로 분리하며, 회의록에 섞인 프롬프트 인젝션에 대비해 LLM 출력은 서버에서 검증한 뒤 저장한다.

## 클라우드 활용 및 배포 계획:

AWS 기반으로 배포한다. 프론트엔드는 S3 + CloudFront로 정적 호스팅하고, 백엔드 API는 Docker 컨테이너로 EC2에서 구동하며 ALB를 통해 HTTPS로 접근한다. 데이터는 RDS(PostgreSQL)에 저장하고, 업로드 파일은 S3에 보관한다.

## 예상 결과물:

HTTPS로 접속 가능한 배포 URL과, 심사 시 즉시 시연할 수 있도록 Owner/Member/Viewer 데모 계정 및 더미 프로젝트를 미리 준비한다. 회원가입 없이 바로 핵심 기능을 확인할 수 있는 상태를 목표로 한다.
